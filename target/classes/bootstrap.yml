spring:
  application:
    name: my_shop
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: DEFAULT_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true